# PostgreSQL Development Configuration
# Only essential overrides for Docker development environment

# Connection Settings
listen_addresses = '*'  # REQUIRED for Docker - Default: 'localhost'

# Optional development settings (uncomment if needed):
# log_statement = 'all'           # Log all SQL statements (verbose!)
# log_duration = on               # Log duration of all statements
# shared_buffers = 128MB          # Can be lower for dev
# max_connections = 50            # Fewer connections needed for dev
