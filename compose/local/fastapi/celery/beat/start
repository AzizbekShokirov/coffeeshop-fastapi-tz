#!/bin/bash

# Start Celery Beat Scheduler in Development Mode

set -o errexit
set -o nounset

echo "⏰ Starting Celery Beat Scheduler (Development)..."
echo "📊 Log level: INFO"

# Remove old schedule file to avoid issues
rm -f /app/celerybeat-schedule

exec celery -A app.tasks.celery_app beat \
    --loglevel=info
